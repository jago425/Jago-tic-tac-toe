// const getFormFields = require(`../../../lib/get-form-fields`)
// const api = require('./api')
// const ui = require('./ui')

// const checkWin = function () { // function to check if x or o has won the game
//   if the values of the indices in the same row match, message that the value wins
//   if the values of the indices in the same column match, message that the value wins
//   if the values of the indices in the diagonals wins, message that the value wins
// }
